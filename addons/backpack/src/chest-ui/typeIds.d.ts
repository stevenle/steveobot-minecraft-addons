/** Vendored from Chest-UI (https://github.com/Herobrine643928/Chest-UI), CC-BY-4.0.
 * Maps vanilla item and block type ids to the numeric ids the chest form UI
 * uses to draw icons. Regenerate from upstream when a game update shifts them. */
export const typeIdToID: Map<string, number>;
export const typeIdToDataId: Map<string, number>;
